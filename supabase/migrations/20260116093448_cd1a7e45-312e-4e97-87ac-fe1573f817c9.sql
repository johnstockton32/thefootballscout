-- Fix remaining security issues (continuation)

-- The first part of the migration succeeded (promo_codes, super_admins, promo_code_redemptions)
-- Now just fix team_activity - the "Users can view their own activity" policy already exists

-- No additional changes needed for team_activity since policies are already in place